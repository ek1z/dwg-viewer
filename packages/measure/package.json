{
  "name": "@dwg-viewer/measure",
  "version": "0.0.0",
  "type": "module",
  "comment": "Phase 2 measurement tools (snapping, distance/area/angle). Pure TS, framework-agnostic. Internal source-consumed package.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "echo 'measure is consumed from source; no standalone build'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@dwg-viewer/dxf-core": "workspace:*",
    "flatbush": "^4.6.2"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^4.1.9"
  }
}
