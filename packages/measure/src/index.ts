/**
 * Phase 2 measurement tools (placeholder).
 *
 * Planned surface: object snapping (endpoint / midpoint / intersection /
 * nearest) backed by an R-tree spatial index, and distance / cumulative / area
 * / angle tools. All measurement math must run on the float64 scene model plus
 * the engine's rebasing offset — never on f32 GPU coordinates (plan §2, §5).
 */

export {};
